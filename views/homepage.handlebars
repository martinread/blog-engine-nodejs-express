
{{#each articles}}
    <div class="card my-3 border-0">
        <div class="card-body">
            <h4 class="card-title" class="articleExample">{{title}}</h4>
            <p>{{content}}</p>
        </div>
    </div>
{{/each}}

           